slam_toolbox:
  ros__parameters:
    use_sim_time: false

    map_frame: "map"
    odom_frame: "odom"
    base_frame: "laser_frame"   # this matches your header.frame_id
    scan_topic: "/scan"

    use_odometry: false
    provide_odom_frame: true
    use_scan_matching: true
    mode: "mapping"

